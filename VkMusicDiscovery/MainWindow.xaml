<Window x:Class="VkMusicDiscovery.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VkMusicDiscovery"
        Title="MainWindow" Height="500" Width="800">
    <Window.Resources>
        <Thickness x:Key="ButThickness">5</Thickness>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="AudioDataGrid" Grid.Column="0" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserReorderColumns="False" MaxWidth="700">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="Auto"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="Auto"/>
                <DataGridCheckBoxColumn/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Column="1" Margin="5">
            <Grid.Resources>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="Margin" Value="{StaticResource ButThickness}"></Setter>
                </Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="{StaticResource ButThickness}"></Setter>
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock VerticalAlignment="Center">Count: </TextBlock>
                <TextBox x:Name="TbCount" VerticalAlignment="Center">10</TextBox>
                <CheckBox x:Name="CbxRandom" VerticalAlignment="Center" IsChecked="True">Random</CheckBox>
            </StackPanel>
            <Button Grid.Row="1" x:Name="BtnRefresh" Click="BtnRefresh_OnClick">Refresh</Button>
            <GroupBox Grid.Row="2" Header="Language" Margin="5,0">
                <StackPanel Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style TargetType="{x:Type RadioButton}">
                            <Setter Property="Margin" Value="{StaticResource ButThickness}"></Setter>
                        </Style>
                    </StackPanel.Resources>
                    <RadioButton x:Name="RbtnLangRu" Checked="RbtnsLang_OnChecked">Ru</RadioButton>
                    <RadioButton x:Name="RbtnLangEng" Checked="RbtnsLang_OnChecked">Eng</RadioButton>
                    <RadioButton x:Name="RbtnLangAll" IsChecked="True" Checked="RbtnsLang_OnChecked">All</RadioButton>
                </StackPanel>
            </GroupBox>
            <Button Grid.Row="3" x:Name="BtnDownloadall" VerticalAlignment="Bottom" FontSize="16" Click="BtnDownloadall_OnClick">Download all</Button>
        </Grid>

    </Grid>
</Window>
